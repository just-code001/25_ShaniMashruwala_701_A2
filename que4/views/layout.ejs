<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= title || 'ERP Admin' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="/css/styles.css" rel="stylesheet" />
</head>
<body>
  <header class="topbar">
    <div class="brand">ERP Admin</div>
    <nav>
      <% if (adminId) { %>
        <a href="/dashboard">Dashboard</a>
        <a href="/employees">Employees</a>
        <form method="POST" action="/logout" style="display:inline;">
          <button class="btn small danger">Logout</button>
        </form>
      <% } %>
    </nav>
  </header>

  <main class="container">
    <% if (success && success.length) { %>
      <div class="alert success"><%= success[0] %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="alert error"><%= error[0] %></div>
    <% } %>

    <%- body %>
  </main>

  <footer class="footer">© <%= new Date().getFullYear() %> ERP</footer>
</body>
</html>
